import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);

//to create android app, run on terminal => eas build --platform android --profile preview
// number register => 3116325708 - Jmerazo96*